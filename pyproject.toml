[project]
name = "splitmix"
version = "0.1.0"
description = "Download and split YouTube audio into tagged tracks"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydub>=0.25.1",
    "mutagen>=1.47.0",
    "streamlit>=1.28.0",
    "yt-dlp>=2024.1.0",
]
